# Copy this file to .env and fill in real values. Never commit .env.

# Base URL of the SmartCart backend API (see backend/src/app.js — every
# feature route is mounted under /api). No trailing slash.
VITE_API_BASE_URL=http://localhost:5000/api
